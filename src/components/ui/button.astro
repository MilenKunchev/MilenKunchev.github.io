---
interface Props {
  size?: "md" | "lg";
  block?: boolean;
  style?: "outline" | "primary" | "inverted";
  class?: string;
  [x: string]: any;
}

const {
  size = "md",
  style = "primary",
  block,
  class: className,
  ...rest
} = Astro.props;

const sizes: Record<"md" | "lg", string> = {
  md: "px-5 py-2.5",
  lg: "px-6 py-3",
};

const styles: Record<"outline" | "primary" | "inverted", string> = {
  outline:
    "border-2 border-neutral-900 text-neutral-900 hover:bg-background-dark hover:text-white",

  primary:
    "bg-background-dark text-white border-2 border-transparent hover:bg-neutral-900",

  inverted:
    "bg-background-surface text-neutral-900 border-2 border-transparent hover:bg-neutral-100",
};
---

<button
  {...rest}
  class:list={[
    "rounded-sm text-center transition focus-visible:ring-2 ring-offset-2 ring-neutral-200",
    block && "w-full",
    sizes[size],
    styles[style],
    className,
  ]}
>
  <slot />
</button>